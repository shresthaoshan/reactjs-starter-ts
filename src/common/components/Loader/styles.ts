import { Space } from "antd";
import styled from "styled-components";

export const LoaderWrapper = styled(Space)`
	width: 100%;
	height: 100%;

	display: flex;
	justify-content: center;
	align-items: center;
`;
